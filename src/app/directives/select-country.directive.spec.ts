import { SelectCountryDirective } from './select-country.directive';

describe('SelectCountryDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectCountryDirective();
    expect(directive).toBeTruthy();
  });
});
